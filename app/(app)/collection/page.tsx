export default function CollectionPage() {
  return (
    <div>
      <h1 className="font-display text-2xl font-bold text-[var(--ink-primary)]">
        Collection
      </h1>
      <p className="mt-2 text-[var(--ink-muted)]">
        Binder editor — ships in Phase 9.
      </p>
    </div>
  );
}
